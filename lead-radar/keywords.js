// Словари для поиска потенциальных клиентов в чатах.
//
// Логика совпадения: сообщение считается лидом, если в нём есть
//   ГЕО (страна/город) + ТЕМА (недвижимость) + НАМЕРЕНИЕ (вопрос или поиск)
// и при этом нет СТОП-СЛОВ (объявления, конкуренты, спам).
//
// Три условия вместо одного — это принципиально. По одному слову «Дубай»
// придёт весь чат; по связке «Дубай + квартира + ищу» приходят те, кто
// действительно ищет. Лучше пропустить половину, чем утонуть в шуме и
// перестать читать уведомления через два дня.

/** Где ищут. Достаточно одного совпадения. Падежи важны: «по Дубаю» — тоже лид. */
export const GEO = [
  "дубай",
  "дубае",
  "дубая",
  "дубаю",
  "дубаем",
  "оаэ",
  "эмират",
  "абу-даби",
  "шардж",
  "аджман",
  // «марина» (Dubai Marina) убрана: это ещё и распространённое женское имя,
  // ловила бы любую Марину в чате.
  "дубай марина",
  "jvc",
  "ереван",
  "еревану",
  "армени",
  "гюмри",
  "тбилиси",
  "тбилисе",
  "батуми",
  "кобулети",
  "грузи",
  "краснодар",
  "сочи",
  "москв",
  "питер",
  "санкт-петербург",
  "спб",
  // Армянский. В ереванских чатах половина пишет латиницей («pntrum em
  // bnakaran»), половина — армянской графикой, и радар не видел ни того,
  // ни другого. Короткие латинские слова берём регуляркой: «tun» подстрокой
  // сидит внутри «fortune», «gin» — внутри «login».
  "երևան",
  "հայաստան",
  "գյումրի",
  "արաբկիր",
  "աջափնյակ",
  "դավթաշեն",
  "մալաթիա",
  "կենտրոն",
  rx(/\by?erevan/i, "erevan"),
  rx(/\bhayastan/i, "hayastan"),
  rx(/\bgyumri/i, "gyumri"),
  rx(/\barabkir/i, "arabkir"),
  rx(/\bach?apnyak/i, "achapnyak"),
  rx(/\bdavtashen/i, "davtashen"),
  rx(/\bkentron/i, "kentron"),
];

/** О чём речь. Достаточно одного совпадения. */
export const TOPIC = [
  "квартир",
  // Запрос часто описывают числом комнат, а слово «квартира» опускают:
  // «хочу купить 2-комнатную в Ереване до $120k». Без этих форм такие
  // сообщения не доходили даже до модели — предфильтр отбрасывал их раньше.
  "комнатн",
  "комнатную",
  "однушк",
  "двушк",
  "трешк",
  "евродвушк",
  "недвижим",
  "новостройк",
  "апартамент",
  "апарт",
  "студи",
  "вилл",
  "пентхаус",
  "жиль",
  "жилье",
  "таунхаус",
  "ипотек",
  "рассрочк",
  "застройщик",
  "золотая виза",
  "резидентск",
  // «метр» отдельно нельзя: слово содержится в «метро», а его в городских
  // чатах упоминают постоянно.
  "квадратн",
  "кв.м",
  "кв. м",
  "м2",
  "за метр",
  // Ищут не только объект, но и того, кто поможет его найти. Такой человек —
  // готовый клиент: он уже решился и подбирает исполнителя.
  // Вопросы про доходность — прямой инвестиционный запрос, лучший тип клиента.
  "инвестиц",
  "доходност",
  "окупаемост",
  "годовых",
  "рентабельн",
  "риэлтор",
  "риелтор",
  "брокер",
  "агент по недвиж",
  "агентство недвиж",
  // Армянский
  "բնակարան",
  "նորակառույց",
  "սենյականոց",
  "սենյակ",
  "բնակելի",
  "տուն",
  rx(/\bbnakaran/i, "bnakaran"),
  rx(/\bnorakaruy(c|ts)/i, "norakaruyc"),
  rx(/\bsenyak/i, "senyak"),
  rx(/\btun\b/i, "tun"),
];

/** Намерение: человек ищет, спрашивает или собирается покупать. */
export const INTENT = [
  "ищу",
  "ищем",
  "посоветуйте",
  "подскажите",
  "подскажет",
  "помогите выбрать",
  "хочу купить",
  "хотим купить",
  "планирую купить",
  "планируем купить",
  "собираюсь купить",
  "рассматриваю",
  "рассматриваем",
  "думаю купить",
  "куда вложить",
  "стоит ли покупать",
  "стоит ли брать",
  "как купить",
  "где купить",
  "какой район",
  "в каком районе",
  "что посоветуете",
  "нужен риэлтор",
  "нужен агент",
  "ищу агента",
  "ищу риэлтора",
  "бюджет",
  "инвестир",
  "переезжа",
  "релокац",
  // Ослабленный набор: обычные вопросительные обороты, с которых начинается
  // разговор о покупке, даже если слов «ищу» и «куплю» в сообщении нет.
  "кто знает",
  "кто сталкивал",
  "кто покупал",
  "кто брал",
  "поделитесь",
  "нужен совет",
  "имеет смысл",
  "выгодно ли",
  "реально ли",
  "сколько стоит",
  "какие цены",
  "почем",
  "по чем",
  "что скажете",
  "какие варианты",
  "есть варианты",
  "смотрю",
  "присматрива",
  "хочу взять",
  "планирую",
  "надежн",
  "проверенн",
  "адеква",
  "хочу",
  "хотим",
  "хотел",
  "хотела",
  "посмотреть",
  "интересует",
  "интересно",
  "подобрать",
  "нужна квартира",
  "нужен вариант",
  // Армянский: «փնտրում եմ բնակարան» / «pntrum em bnakaran»
  "փնտրում եմ",
  "փնտրում ենք",
  "ուզում եմ",
  "ուզում ենք",
  "խորհուրդ տվեք",
  "հետաքրքրում է",
  "բրոկեր",
  "ռիելտոր",
  "գործակալ",
  rx(/\bpntrum\s+e(m|nq)/i, "pntrum em"),
  rx(/\buzum\s+e(m|nq)/i, "uzum em"),
  rx(/\bxorhurd\s+tvek/i, "xorhurd tvek"),
  rx(/\brieltor/i, "rieltor"),
  rx(/\bgorcakal/i, "gorcakal"),
];

/**
 * Шум: объявления, конкуренты, реклама. Одного совпадения достаточно,
 * чтобы отбросить сообщение целиком.
 */
export const STOP = [
  "сдаю",
  "сдам",
  "сдается",
  "сдаётся",
  "продаю",
  "продам",
  "продается",
  "продаётся",
  "продажа от собственника",
  // «агентство недвижимости» отсюда убрано намеренно: под эту фразу попадало
  // и «посоветуйте агентство недвижимости в Дубае» — то есть готовый клиент.
  // Рекламу конкурентов отсекаем по признакам рекламы, а не по слову «агентство».
  "наш канал",
  "подписывайтесь",
  "подпишись",
  "реклама",
  "промокод",
  "скидка только сегодня",
  "пишите в личку по вопросам сотрудничества",
  "куплю базу",
  "разместить объявление",
  "вакансия",
  "резюме",
  // Поиск работы. Такие сообщения полны слов «ищу», «рассматриваю» и «ВНЖ» —
  // то есть попадают во все наши списки, не имея к покупке никакого отношения.
  "ищу работу",
  "ищу вакансию",
  "подработ",
  "частную работу",
  "ищу попутчик",
  "нужна работа",
  "в поисках работы",
  "возможность работать",
  "готов работать",
  "готова работать",
  "рассматриваю вакансии",
  "трудоустройств",
  "работодател",
  "опыт работы",
  "беженц",
  "ищу клиентов",
  "ищу заказы",
  "предлагаю услуги",
  "оказываю услуги",
  "крипт",
  "заработок",
  "ставки",
  // Туристический шум: город и сумма в одном сообщении ещё не значат покупку.
  // Осторожно с обрезками: «отел» содержится в «хотел» и убивал бы
  // «хотел бы купить квартиру в Дубае» — то есть лучший из возможных лидов.
  "отель",
  "отеля",
  "отеле",
  "хостел",
  "авиабилет",
  "билеты",
  "рейс",
  "такси",
  "ресторан",
  "кафе",
  // Соседний бизнес, который цепляется словом «рассрочка» и городом:
  // «Авто в Дубае в рассрочку» проходило фильтр три раза подряд.
  "автопарк",
  "авто в рассрочку",
  "аренда авто",
  "прокат авто",
  // Набор агентов в штат конкурента. Слов «недвижимость» и «сотрудничество»
  // там в избытке, а клиента — ни одного.
  "расширением команды",
  "ищем специалистов",
  "ищем агентов",
  "ищем брокеров",
  "форматы сотрудничества",
  "приглашаем к сотрудничеству",
  // Витрина объекта: это предложение, а не запрос.
  "предлагается к продаже",
  "эксклюзивная вилла",
  // «Подскажите, на каком сайте искать квартиры» — человек ищет портал,
  // а не агента. Отвечать нечем, кроме ссылки на чужой сайт.
  "на каком сайте",
  "какой сайт",
  "какие сайты",
  "через какой сайт",
  // Ищут человека в команду. Слова «инвестиции» и «ОАЭ» там есть,
  // недвижимости — нет.
  "продюсер",
  "личный бренд",
  "личныйбренд",
  // Армянский: продавец и тот, кто ищет работу. «փնտրում եմ» есть у обоих,
  // поэтому различает их только предмет поиска.
  "վաճառվում է",
  "վաճառում եմ",
  "աշխատանք փնտրում",
  "աշխատավարձ",
  "աշխատանք եմ փնտրում",
  rx(/\bvacharvum/i, "vacharvum"),
  rx(/\bvacharum\s+em/i, "vacharum em"),
  rx(/\bashxatanq/i, "ashxatanq"),
  rx(/\bashxatavardz/i, "ashxatavardz"),
];

/**
 * Слабая тема. «ВНЖ» и «вид на жительство» пишут и покупатели недвижимости,
 * и мигранты в поисках работы — само по себе это слово ничего не значит.
 * Засчитывается только вместе с настоящей темой или названной суммой.
 */
export const WEAK_TOPIC = ["внж", "вид на жительство", "гражданств"];

/**
 * Аренда. Главный источник шума: чаты по недвижимости на 90% состоят из
 * «сниму» и «сдам», а вы продаёте. В стоп-лист это класть нельзя —
 * «куплю квартиру для сдачи в аренду» тоже содержит слово «аренда»,
 * и это отличный инвестиционный клиент. Поэтому аренда отбрасывает
 * сообщение, только если рядом нет признака покупки.
 */
export const RENTAL = [
  "аренд",
  "снять",
  "сниму",
  "снимаю",
  "снимем",
  "съем",
  "посуточн",
  "долгосрочн",
  "в месяц",
  "/мес",
  "на месяц",
  "на два месяца",
  "на полгода",
  "квартирант",
  "залог",
  "коммуналк",
  // Разбор реальных находок показал: слов «снять» и «аренда» в объявлениях
  // почти нет. Люди описывают съём по-другому — сроком проживания, составом
  // семьи и обещанием быть хорошими жильцами. Именно на этих формулировках
  // аренда и просачивалась мимо фильтра.
  "на длительный срок",
  "на долгий срок",
  "длительный срок",
  "долгий срок",
  "на год",
  "на полгода",
  "заселит",
  "заехат",
  "готов заех",
  "готовы заех",
  "проживать буд",
  "проживания",
  "проживать",
  "без вредных привычек",
  "без в/п",
  "с животными",
  "с кошк",
  "с собак",
  "подселени",
  "хозяин квартиры",
  "предоплата за",
  "первый и последний месяц",
  // Комнату и короткий срок снимают, а не покупают. «Комнату» безопасно:
  // в «2-комнатную» этой формы нет, а «куплю комнату» всё равно пройдёт
  // по признаку покупки.
  "комнату",
  "на неделю",
  "на несколько дней",
  "на пару недель",
  // Армянский. Голый корень «վարձ» брать нельзя: он сидит внутри
  // «աշխատավարձ» — это зарплата, а не аренда.
  "վարձով",
  "վարձակալ",
  "վարձել",
  "ամսական",
  "օրավարձ",
  rx(/\bvardzov/i, "vardzov"),
  rx(/\bvardzakal/i, "vardzakal"),
  rx(/\bamsakan/i, "amsakan"),
  rx(/\boravardz/i, "oravardz"),
];

/**
 * Признаки арендного чата в его названии. Такие чаты состоят из сдачи
 * и съёма целиком, и обычные слова «квартира в Ереване» там не значат
 * ничего. Пропускаем оттуда только сообщения с явным признаком покупки.
 */
export const RENTAL_CHAT = ["аренд", "rent", "сдам", "сниму", "съем", "վարձ", "vardz"];

/** Признаки именно покупки — они перевешивают упоминание аренды. */
export const BUY_SIGNAL = [
  "купить",
  "куплю",
  "покупк",
  "покупа",
  "приобрет",
  "инвестиц",
  "инвестир",
  "вложить",
  "вложен",
  "ипотек",
  "рассрочк",
  "застройщик",
  // «новостройка» сюда не годится: объявления о сдаче сплошь «Квартира
  // в Новостройке», и это слово в одиночку пробивало фильтр аренды.
  // Тип дома ничего не говорит о том, покупают его или снимают.
  "в собственность",
  "на этапе строит",
  // Инвестор спрашивает «какая доходность аренды» — слово «аренда» там есть,
  // но это разговор о покупке, а не о съёме.
  // Только однозначно инвесторская лексика. «Сдавать» и «под сдачу» сюда
  // не годятся: в арендном чате так пишут арендодатели, а не покупатели.
  // «Куплю под сдачу» и без них проходит — там есть слово «куплю».
  "доходност",
  "окупаемост",
  "годовых",
  "рентабельн",
  // Армянский
  "գնել",
  "գնում եմ",
  "առնել",
  "հիփոթեք",
  "ապառիկ",
  "ներդրում",
  "գնորդ",
  rx(/\bgnel/i, "gnel"),
  rx(/\bgnum\s+e(m|nq)/i, "gnum em"),
  rx(/\barnel/i, "arnel"),
  rx(/\bhipotek/i, "hipotek"),
  rx(/\baparik/i, "aparik"),
  rx(/\bnerdrum/i, "nerdrum"),
];

/** Срочность — сильный признак горячего клиента. */
const URGENCY = [
  "срочно",
  "в ближайш",
  "в этом месяце",
  "до конца года",
  "уже смотрю",
  "готов выйти на сделку",
];

/** Самые весомые слова намерения — по ним считаем находку горячей. */
const STRONG_INTENT = [
  "ищу",
  "ищем",
  "хочу купить",
  "хотим купить",
  "планирую купить",
  "собираюсь купить",
  "нужен риэлтор",
  "нужен агент",
  "куда вложить",
  "ուզում եմ գնել",
  "փնտրում եմ բնակարան",
  rx(/\buzum\s+em\s+gnel/i, "uzum em gnel"),
  rx(/\bpntrum\s+em\s+bnakaran/i, "pntrum em bnakaran"),
];

/** Минимальная длина сообщения. Короткие реплики чаще мусор, но не всегда. */
export const MIN_LENGTH = 15;

/**
 * Упоминание денег — сильный сигнал намерения даже без слов «ищу» и «куплю».
 * «Есть что-то в районе 90к?» или «до 250 000$» — это разговор о покупке.
 */
// Граница слова \b в JavaScript считает «словом» только латиницу и цифры,
// поэтому после кириллической «к» она не срабатывает и «60к» не находилось.
// Вместо \b — явная проверка, что дальше не идёт буква.
const MONEY_RE =
  /\d[\d\s.,]*\s*(к(?![а-яё])|k(?![a-z])|тыс|млн|000|\$|€|₽|долл|dollar|евро|руб|драм|դրամ|dram(?![a-z])|лари|дирх)/i;

/**
 * Ниже этой суммы речь почти наверняка об аренде, а не о покупке.
 *
 * Раньше сумма проверялась только на факт наличия: «350 000 драм» и
 * «$350 000» давали одинаковые +2 балла. Но 350 тысяч драм — это ≈$900,
 * то есть месячная аренда, а покупка в Ереване начинается от 15 миллионов.
 * Без учёта величины арендные объявления проходили фильтр по своему же
 * ценнику.
 */
const PURCHASE_FLOOR = {
  amd: 5_000_000,
  usd: 20_000,
  eur: 20_000,
  rub: 1_000_000,
  gel: 50_000,
  aed: 300_000,
};

const CURRENCY_ALIASES = [
  [/^(драм|դրամ|dram|amd)/i, "amd"],
  [/^(dollar)/i, "usd"],
  [/^(\$|долл|usd)/i, "usd"],
  [/^(€|евро|eur)/i, "eur"],
  [/^(₽|руб|rub)/i, "rub"],
  [/^(лари|gel)/i, "gel"],
  [/^(дирх|aed)/i, "aed"],
];

function currencyCode(token) {
  if (!token) return null;
  for (const [re, code] of CURRENCY_ALIASES) if (re.test(token)) return code;
  return null;
}

// Валюта может стоять и до числа ($120k), и после (350к драм). Множитель —
// «к», «тыс» или «млн». Пробелы внутри числа обычны: «350 000».
const AMOUNT_RE =
  /([$€₽])?\s*(\d[\d\s]*(?:[.,]\d{3})*(?:[.,]\d{1,2})?)\s*(к(?![а-яё])|k(?![a-z])|тыс|млн)?\s*([$€₽]|долл|dollar|евро|руб|драм|դրամ|dram|лари|дирх|usd|eur|aed|amd|gel)?/gi;

const MULTIPLIER = { к: 1e3, k: 1e3, тыс: 1e3, млн: 1e6 };

/**
 * Число из текста чата. Точка и запятая тут значат разное в зависимости от
 * того, сколько знаков идёт следом: «250.000» — это 250 тысяч, а «26,8 млн» —
 * двадцать шесть с половиной миллионов. Считать «250.000» за 250 опасно
 * вдвойне: «$250.000» — это покупка, и она уехала бы в отсев как аренда.
 */
function parseNumber(raw) {
  return Number(
    raw
      .replace(/\s/g, "")
      .replace(/[.,](\d{3})(?!\d)/g, "$1")
      .replace(",", ".")
  );
}

/** Все суммы с распознанной валютой. Числа без валюты пропускаем: неясно, чего. */
export function extractAmounts(haystack) {
  const found = [];
  for (const m of haystack.matchAll(AMOUNT_RE)) {
    const currency = currencyCode(m[1]) ?? currencyCode(m[4]);
    if (!currency) continue;

    const digits = parseNumber(m[2]);
    if (!Number.isFinite(digits) || digits === 0) continue;

    const mult = m[3] ? MULTIPLIER[m[3].toLowerCase()] ?? 1 : 1;
    found.push({ value: digits * mult, currency });
  }
  return found;
}

/**
 * Похоже ли, что названная сумма — это арендная плата, а не цена покупки.
 * Смотрим на максимум: в «двушка до 300 000 драм, депозит 150 000» решает
 * большая сумма.
 */
export function looksLikeRentPrice(haystack) {
  // Цена за квадратный метр всегда мала — по ней о покупке судить нельзя.
  if (/за\s*(кв|м2|м²|квадрат)|\/\s*м2|\/\s*м²/i.test(haystack)) return false;

  const amounts = extractAmounts(haystack);
  if (!amounts.length) return false;

  return amounts.every((a) => a.value < PURCHASE_FLOOR[a.currency]);
}

/**
 * Голос продавца. Покупатель и агент пишут одними словами — «Дубай»,
 * «новостройка», «рассрочка», «первый взнос», — и по словарю их не различить.
 * Но агент проговаривается тем, что ПРЕДЛАГАЕТ, а не спрашивает.
 */
export const SELLER_VOICE = [
  "цены стартуют",
  "цена стартует",
  "старт продаж",
  "можем помочь",
  "чем еще можем помочь",
  "оформление виз",
  "бесплатный подбор",
  "бесплатный поиск",
  "подбор жилья",
  "подбор недвижимости",
  "квартиры на продажу",
  "объекты на продажу",
  "есть на продажу",
  "первый взнос от",
  "рассрочка 0",
  "право наследования",
  "property insurance",
  "наше агентство",
  "наша компания",
];

/**
 * Объявление узнаётся по вёрстке, а не по словам, и это надёжнее всего.
 *
 * Карточка объекта — это поля («Локация: AL Jadaf», «Срок сдачи: 2027») и
 * списки с эмодзи. Человек, который ищет квартиру, пишет одной репликой:
 * «ребят, ищу в Дубае до 370к, что посоветуете». Он не верстает.
 *
 * Поэтому смотрим на структуру: много строк вида «поле: значение» или
 * много строк-пунктов — значит, это витрина, кем бы она ни была подписана.
 */
export function looksLikeListing(text) {
  const lines = text
    .split("\n")
    .map((l) => l.trim())
    .filter(Boolean);
  if (lines.length < 5) return false;

  const fieldLines = lines.filter((l) => /^[^:\n]{2,40}:\s*\S/.test(l)).length;
  const bulletLines = lines.filter((l) =>
    /^([—–\-•*]|\p{Extended_Pictographic})/u.test(l)
  ).length;

  return fieldLines >= 3 || bulletLines >= 4;
}

/** ё → е, всё в нижний регистр: иначе «жильё» и «жилье» — разные слова. */
export function normalize(text) {
  return text.toLowerCase().replace(/ё/g, "е");
}

/**
 * Слово-регулярка с подписью. Нужна для армянской латиницы: короткие
 * «tun» (дом) и «gin» (цена) подстрокой сидят внутри «fortune» и «login»,
 * а обычный поиск по вхождению этого не различает. Подпись уходит
 * в уведомление — по ней видно, за что радар зацепился.
 */
function rx(re, label) {
  return { re, label };
}

function findMatches(haystack, list) {
  return list
    .filter((item) => (item.re ? item.re.test(haystack) : haystack.includes(item)))
    .map((item) => item.label ?? item);
}

/**
 * Разбирает сообщение. Возвращает null, если это не лид, иначе —
 * какие именно слова сработали (их полезно видеть в уведомлении,
 * чтобы на ходу подкручивать словари).
 *
 * chatTitle нужен вот зачем: в чате «Русские в Дубае» страну вслух не называют,
 * она очевидна из контекста. Там пишут просто «думаю купить студию, что
 * посоветуете». Если требовать гео только в тексте, такие сообщения — а это
 * лучшие лиды — теряются полностью. Поэтому название чата тоже считается за гео.
 */
export function matchLead(text, chatTitle = "") {
  if (!text || text.length < MIN_LENGTH) return null;

  const haystack = normalize(text);

  const stop = findMatches(haystack, STOP);
  if (stop.length) return null;

  // Реклама объекта от агента. Проверяем и голос, и вёрстку: голос ловит
  // короткие предложения услуг, вёрстка — длинные карточки с полями,
  // где ни одного «продающего» слова может и не быть.
  if (findMatches(haystack, SELLER_VOICE).length) return null;
  if (looksLikeListing(text)) return null;

  // Аренда без признаков покупки — не наш клиент. Это отсеивает и объявления
  // «сдам 2-комн», и тех, кто ищет жильё на два месяца.
  const buySignals = findMatches(haystack, BUY_SIGNAL);
  const rentalChat = findMatches(normalize(chatTitle), RENTAL_CHAT).length > 0;
  // Сумма размером с месячную аренду — такой же признак съёма, как слово
  // «сниму», и куда более надёжный: ценник в объявлении есть почти всегда.
  const rentPrice = looksLikeRentPrice(haystack);
  if (
    (findMatches(haystack, RENTAL).length || rentalChat || rentPrice) &&
    !buySignals.length
  ) {
    return null;
  }

  const geo = findMatches(haystack, GEO);
  const geoFromChat = findMatches(normalize(chatTitle), GEO);
  if (!geo.length && !geoFromChat.length) return null;

  const money = MONEY_RE.test(haystack);
  const topic = findMatches(haystack, TOPIC);

  // «В Батуми есть что-то в районе 60к?» — слова «квартира» нет, но город
  // назван прямо и озвучен бюджет. В чатах так спрашивают постоянно, поэтому
  // сумма заменяет тему. Условий три, и все нужны: город в самом тексте
  // (названия чата мало), знак вопроса и короткое сообщение — иначе под
  // правило подпадает любое объявление с городом и ценником.
  const moneyInsteadOfTopic =
    money && geo.length && haystack.includes("?") && text.length < 200;
  if (!topic.length && !moneyInsteadOfTopic) return null;
  if (!topic.length) topic.push("названа сумма вместо предмета");

  // Слабая тема идёт в отчёт только как дополнение — сама по себе она сюда
  // уже не приводит, поскольку без настоящей темы мы вышли строкой выше.
  topic.push(...findMatches(haystack, WEAK_TOPIC));

  // Вопрос без слов-намерений тоже считается: «кто покупал квартиру в Батуми?»
  const intent = findMatches(haystack, INTENT);
  // Намерение засчитывается тремя путями: слово из списка, вопрос
  // или упоминание суммы. Раньше было только первые два — из-за этого
  // терялись реплики вроде «в Батуми есть что-то в районе 60к».
  if (!intent.length && !haystack.includes("?") && !money) return null;

  if (money && !intent.length) intent.push("упомянута сумма");

  // Оценка «горячести». Нужна, чтобы в потоке было видно, кому отвечать
  // первым, и чтобы разбор истории присылал только лучшее, а не всё подряд.
  let score = 0;
  if (buySignals.length) score += 3;
  // Сумму засчитываем, только если она похожа на цену покупки: до сюда
  // доходят и объявления с арендным ценником, если в них есть слово «куплю».
  if (money && !rentPrice) score += 2;
  if (findMatches(haystack, STRONG_INTENT).length) score += 2;
  if (geo.length) score += 1; // город назван прямо, а не взят из чата
  score += Math.min(topic.length, 2);
  if (findMatches(haystack, URGENCY).length) score += 1;

  const level = score >= 6 ? "hot" : score >= 4 ? "warm" : "cold";

  return {
    geo: geo.length ? geo : geoFromChat.map((g) => `${g} (из названия чата)`),
    topic,
    intent,
    buySignals,
    score,
    level,
    /** Значок для уведомления — видно с первого взгляда. */
    badge: level === "hot" ? "🔥" : level === "warm" ? "🟡" : "⚪️",
  };
}
